"use client";

import PlaceholderPanel from "./PlaceholderPanel";

export default function MediumPanel() {
  return (
    <PlaceholderPanel
      name="Medium Analytics"
      description="Connect your Medium account to view reads, views, and audience stats."
    />
  );
}
